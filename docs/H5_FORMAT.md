# Institute HDF5 schema

Sources: `ibo-biomech/handlers/h5Handler.py`, `biomech_io/file_converter.py`, containers, installed backend, and two actual H5 files inspected with h5py. This is not a generic HDF5 importer.

| Path                                       | Stored shape / attributes                                                                                                           | Meaning and reader behaviour                                                                                                           |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `/MetaData`                                | attributes Project, ProjectPI, SubjectID, Condition, BodyMass, BodyHeight, Sex, Age, OriginalFiles, PathFile, creation/update times | Arbitrary metadata; often the string Unknown. Python copies group attributes. Never sent over the network.                             |
| `/MetaData/Location`                       | Lat, Lon                                                                                                                            | Optional metadata, not lab coordinates.                                                                                                |
| `/Trajectories`                            | SamplingFrequency, NumFrames, StartFrame, EndFrame, GlobalCoordinateSystem                                                          | SamplingFrequency required for timing. Coordinate description is empty in both inspected files. No explicit timestamp dataset.         |
| `/Trajectories/Labeled`                    | Labels array, optional NumLabeled, Unit                                                                                             | Labels ordered as first dataset dimension. Unit is mm in converted file; absent in older file. Python ignores Unit and defaults to mm. |
| `.../Data`                                 | `[markers,4,frames]` float64                                                                                                        | XYZ followed by homogeneous 1 from converter, NOT a residual or validity flag.                                                         |
| `.../Type`                                 | `[markers,frames]` int8                                                                                                             | Converter writes ones; reader ignores. Measured/virtual coding is not established.                                                     |
| `.../Residuals`                            | optional `[1,markers,frames]`                                                                                                       | Converter copies ezc3d residuals. Negative values indicate invalid C3D points. Older file lacks this dataset.                          |
| `/Trajectories/Unlabeled`                  | NumUnlabeled, empty Data/Type                                                                                                       | Converter emits empty arrays; Python does not load.                                                                                    |
| `/Analog`                                  | Labels, SamplingFrequency, SamplingFactor, NumSamples, BoardName                                                                    | Data already scaled; units not written by inspected converter. SamplingFactor is redundant, not the time authority.                    |
| `/Analog/Data`                             | `[channels,samples]`                                                                                                                | Original-rate scalar signals, including EMG where present.                                                                             |
| `/ForcePlates/{key}`                       | Name, SamplingFrequency, SamplingFactor, NumSamples, CoordinateSystem, Filter, unit_force, unit_moment, unit_position, origin       | Independent rate. NumSamples may be incorrect (3 instead of 4230). Use actual array shape.                                             |
| `.../Force`, `.../Moment`, `.../COP`       | `[3,samples]` observed; reader comments mention `[samples,3]`                                                                       | Already computed vectors. Preserve values; convert declared units only.                                                                |
| `.../Tz`                                   | `[samples]`, optional in older file                                                                                                 | Free moment, not vertical force despite a container comment.                                                                           |
| `.../Location`                             | `[3,4,frames]`                                                                                                                      | Global corners repeated at marker rate by converter. May instead be static `[3,4]` or `[3,4,1]`.                                       |
| `.../Position`                             | `[3,frames]`; older `[4,frames]`                                                                                                    | Exporter repeats local sensor offset here, NOT reliable global plate centre. Do not use this to place a global GRF.                    |
| `.../Rotation`                             | observed `[3,3,3]` all zeros                                                                                                        | Exporter mistakenly uses component count as sample count. Not a valid rotation.                                                        |
| `.../Offset`                               | `[3]` all zeros                                                                                                                     | Placeholder in exporter. No further semantics established.                                                                             |
| `/RigidBodies`, `/Events`, `/CustomFields` | empty in both samples                                                                                                               | Preserved by Python save, not interpreted by load. No supported event or skeleton schema established.                                  |

## Observed variants

The matching converted pair has 31 markers, 193 frames at 120 Hz, 12 analogs and two plates at 960 Hz (1544 samples). Stored H5 frame origin is 336, matching ezc3d's zero-based first frame, whereas raw C3D first frame is 337. It has Unit, Residuals and Tz.

The older virtual-marker file has 69 markers, 423 frames at 200 Hz, 56 analogs and five plates at 2000 Hz (4230 samples). Unit, residuals and Tz are absent. NumSamples incorrectly says 3; Position has four rows. Its source C3D has 68 markers and a different frame origin. It must not be treated as an unchanged equivalent trial.

## Explicit, inferred and calculated

Explicit: trajectories, labels, rates, force streams, corners, unit attributes where present, optional residuals, opaque metadata. Inferred: time starts at zero and regular sampling; missing marker unit follows Python's mm default with a warning; Location sampled at marker rate when its sample count matches markers, at force rate when matching forces, or static when all corners are identical. Unrecognized dynamic geometry timing is omitted with a warning.

Calculated after reading: metre/N/Nm normalization, validity from finite XYZ and negative residuals, timeline alignment during display. Type and the homogeneous fourth row are ignored by normalization; they are not interpreted as validity rules. The original file is never rewritten.

CoordinateSystem uses documented container codes 1=global, 0=local, but the converter writes 0 for already-global ezc3d outputs. Web policy: code 1 is global; known converter layout with zero placeholder rotations uses the reference's stored-global convention with a visible warning. Other code-0 layouts require an explicit user choice of stored-global interpretation before spatial force rendering; force signals remain inspectable. No speculative double rotation.

No H5 event semantics have been established. Nonempty Events is reported as unsupported instead of fabricating times.
